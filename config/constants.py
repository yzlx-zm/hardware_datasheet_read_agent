# ================= 全局常量配置 =================

# 归档文档固定章节结构（与Prompt强制结构完全一致，用于去重、合并、校验）
ARCHIVE_CHAPTERS = [
    "1. 文档概述",
    "2. 物理层通信参数",
    "3. 链路层数据帧结构",
    "4. 核心指令集汇总",
    "5. 校验算法定义",
    "6. 模块全生命周期工作时序",
    "7. 特殊时序与约束标记",
    "8. 典型交互示例",
    "9. 错误码汇总表"
]

# 主标题匹配前缀
ARCHIVE_TITLE_PREFIX = "# "

# 二级章节匹配前缀
CHAPTER_PREFIX = "## "

# 表格章节列表（需要检查表格格式的章节）
TABLE_CHAPTERS = [
    "3. 链路层数据帧结构",
    "4. 核心指令集汇总",
    "9. 错误码汇总表"
]

# 关键数据提取模式（用于数据一致性检查）
KEY_DATA_PATTERNS = {
    "baud_rate": r"波特率[：:]\s*(\d+)",
    "baud_rate_alt": r"(\d{4,8})\s*bps",
    "frame_header": r"(0x[0-9A-Fa-f]{2,4})\s*(?:帧头|头部|起始)",
    "register_addr": r"(?:寄存器|地址)[：:\s]*(0x[0-9A-Fa-f]{2,8})",
    "data_bits": r"数据位[：:]\s*(\d+)",
    "stop_bits": r"停止位[：:]\s*(\d+)",
    "parity": r"校验位[：:]\s*(\w+)",
    "error_code": r"(0x[0-9A-Fa-f]{2})\s*[：:|]",
}
