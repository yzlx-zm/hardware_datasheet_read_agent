# 3D人脸识别模组 通信协议归档文档

## 1. 文档概述
- 文档版本：V1.5
- 适用模组型号：文档未明确说明
- 文档核心范围：3D人脸识别模组与主控之间的串口通信协议。
- 修订记录：文档未明确说明

## 2. 物理层通信参数
- 硬件接口类型：串口
- 默认波特率：115200
- 数据位：8
- 停止位：1
- 校验位：不使用
- 流控方式：不使用

## 3. 链路层数据帧结构
1.  **帧结构**

| 字段名称 | 字节偏移 | 长度 | 功能说明 | 固定值/示例值 |
| :--- | :--- | :--- | :--- | :--- |
| SyncWord | 0 | 2 bytes | 消息开头同步字 | 0xEF 0xAA |
| MsgID | 2 | 1 byte | 消息ID | 例如：0x10 (MID_RESET) |
| Size | 3 | 2 bytes | Data字段的长度（单位：字节） | 高位在前 |
| Data | 5 | N bytes | 消息对应的数据，N=0表示无参数 | 详见各指令说明 |
| ParityCheck | 5+N | 1 byte | 奇偶校验码 | 计算方式见第5章 |

2.  **字节序规则**
    - Size字段（2字节）采用**大端序（高位在前）**。

## 4. 核心指令集汇总
| 指令名称 | 十六进制指令码 | 核心功能说明 |
| :--- | :--- | :--- |
| MID_REPLY | 0x00 | 模块对主控命令的应答 |
| MID_NOTE | 0x01 | 模块主动发给主控的信息 |
| MID_RESET | 0x10 | 停止所有当前处理，模块进入standby状态 |
| MID_GETSTATUS | 0x11 | 立即返回模块当前状态 |
| MID_VERIFY | 0x12 | 鉴权解锁 |
| MID_ENROLL | 0x13 | 交互录入 |
| MID_ENROLL_SINGLE | 0x1D | 单帧录入 |
| MID_ENROLL_BY_FEATURE | 0x1F | 特征值录入 |
| MID_DELUSER | 0x20 | 删除一个注册用户 |
| MID_DELALL | 0x21 | 删除所有注册用户 |
| MID_FACERESET | 0x23 | 重置算法状态，清空已录入的方向 |
| MID_GET_ALL_USERID | 0x24 | 获取所有已注册用户的数量和ID |
| MID_ENROLL_ITG | 0x26 | 集成支持并扩展所有录入方式 |
| MID_GET_VERSION | 0x30 | 获得软件版本信息 |
| MID_POWERDOWN | 0xED | 模块准备关机，结束当前任务，清除文件系统缓存 |
| MID_DEMOMODE | 0xFE | 进入演示模式 |

## 5. 校验算法定义
- **校验方式**：奇偶校验码（ParityCheck）
- **计算范围**：整条协议除去SyncWord（0xEF, 0xAA）部分后，其余所有字节。
- **详细计算规则**：对计算范围内的所有字节按位进行**异或（XOR）** 运算，得到1字节的校验码。

## 6. 模块全生命周期工作时序
1.  **上电初始化时序**：主控给模组上电 -> 模组启动 -> 进入standby状态 -> 模组主动向主控发送NID_READY类型的NOTE消息。
2.  **正常工作模式主流程**：主控收到NID_READY后，开始发送命令消息 -> 模组处理并返回结果（REPLY或NOTE）。
3.  **核心功能（录入/解锁/删除）的执行时序**：
    - **录入**：主控发送录入指令 -> 模组实时返回人脸状态NOTE -> 录入成功/失败后返回REPLY。
    - **解锁**：主控发送解锁指令 -> 模组实时返回人脸状态NOTE -> 鉴权成功/失败后返回REPLY。
    - **删除**：主控发送删除指令 -> 模组处理完成后返回REPLY。
4.  **异常处理时序**：指令处理超时 -> 主控可选择发送MID_GETSTATUS查询状态、发送MID_RESET复位指令或断电重启。
5.  **掉电时序**：主控发送MID_POWERDOWN指令 -> 模组处理并返回REPLY -> 主控可安全断电。

## 7. 特殊时序与约束标记
- 【⚠️ 关键时序约束】主控发送的每条命令，模块最终都会进行REPLY应答。
- 【⚠️ 关键时序约束】当指令处理超时，主控可采用发送MID_GETSTATUS指令、发送RESET指令或断电重启的处理方式。
- 【⚠️ 关键时序约束】录入过程中可通过FACE RESET指令终止录入，先前的录入状态也会清零。
- 【⚠️ 关键时序约束】如果在录入过程中突然断电，之前录入的人脸不会保存。
- 【⚠️ 关键时序约束】模块拿到的第一帧图片就录入成功时，将直接以REPLY消息的形式返回结果，不发送过程NOTE。
- 【⚠️ 关键时序约束】人脸录入时，我们建议用户在实际录入的过程中，缓缓向某个方向转头，并且保持偏转，请不要过快的转头或者立刻回归正脸状态。

## 8. 典型交互示例
### 示例1：模组复位指令交互
- **场景说明**：主控发送复位指令，使模组停止当前所有任务并进入待机状态。
- **主控发送帧**：`EF AA 10 00 00 10`
    - `EF AA`: 同步头。
    - `10`: 指令码 MID_RESET。
    - `00 00`: Data长度为0。
    - `10`: 校验码。计算：`0x10 XOR 0x00 XOR 0x00 = 0x10`。
- **模组正常应答帧**：`EF AA 00 00 02 10 00 12`
    - `EF AA`: 同步头。
    - `00`: 指令码 MID_REPLY。
    - `00 02`: Data长度为2字节。
    - `10`: 回复对应的原始指令码 MID_RESET。
    - `00`: 执行结果 MR_SUCCESS。
    - `12`: 校验码。计算：`0x00 XOR 0x00 XOR 0x02 XOR 0x10 XOR 0x00 = 0x12`。

### 示例2：单用户删除指令交互
- **场景说明**：主控发送指令，删除用户ID为1的已注册用户。
- **主控发送帧**：`EF AA 20 00 02 01 00 23`
    - `EF AA`: 同步头。
    - `20`: 指令码 MID_DELUSER。
    - `00 02`: Data长度为2字节。
    - `01`: 用户ID高八位 (user_id_heb)。
    - `00`: 用户ID低八位 (user_id_leb)。组合后用户ID为 `0x0100`？**注意**：文档示例中ID为1时表示为`0x00 0x01`（小端），此处按文档表格描述（高八位在前）构造，可能存在歧义，实际应以头文件或稳定行为为准。此处仅为演示格式。
    - `23`: 校验码。计算：`0x20 XOR 0x00 XOR 0x02 XOR 0x01 XOR 0x00 = 0x23`。
- **模组正常应答帧**：`EF AA 00 00 02 20 00 02`
    - `EF AA`: 同步头。
    - `00`: 指令码 MID_REPLY。
    - `00 02`: Data长度为2字节。
    - `20`: 回复对应的原始指令码 MID_DELUSER。
    - `00`: 执行结果 MR_SUCCESS。
    - `02`: 校验码。计算：`0x00 XOR 0x00 XOR 0x02 XOR 0x20 XOR 0x00 = 0x02`。

### 示例3：人脸解锁鉴权指令交互
- **场景说明**：主控发送鉴权指令，要求解锁成功后不立刻断电，超时时间设为10秒。
- **主控发送帧**：`EF AA 12 00 02 00 0A 1E`
    - `EF AA`: 同步头。
    - `12`: 指令码 MID_VERIFY。
    - `00 02`: Data长度为2字节。
    - `00`: pd_rightaway参数，0表示解锁成功后不立刻断电。
    - `0A`: timeout参数，0x0A = 10秒。
    - `1E`: 校验码。计算：`0x12 XOR 0x00 XOR 0x02 XOR 0x00 XOR 0x0A = 0x1E`。
- **模组正常应答帧（成功）**：`EF AA 00 00 25 12 00 01 00 41 6C 69 63 65 ... 00 2A`
    - `EF AA`: 同步头。
    - `00`: 指令码 MID_REPLY。
    - `00 25`: Data长度为37字节 (0x25)。
    - `12`: 回复对应的原始指令码 MID_VERIFY。
    - `00`: 执行结果 MR_SUCCESS。
    - `01 00`: 已验证用户的ID，示例为 `0x0100`？（同示例2备注）。
    - `41 6C 69 63 65 ...`: 32字节的用户名字”Alice...“。
    - `00`: Admin标志，0表示不是管理员。
    - `00`: unlockStatus (暂未使用)。
    - `2A`: 校验码（示例值，实际计算得出）。
- **异常应答说明**：若解锁失败（如无人脸、活体检测失败等），模组返回的REPLY中`result`字段将为非0的错误码，且`data`部分可能不包含用户ID和姓名。

## 9. 错误码汇总表
| 错误码名称 | 十六进制值 | 十进制值 | 错误含义说明 |
| :--- | :--- | :--- | :--- |
| MR_SUCCESS | 0x00 | 0 | 成功 |
| MR_REJECTED | 0x01 | 1 | 模块拒绝该命令 |
| MR_ABORTED | 0x02 | 2 | 录入/解锁算法已终止 |
| MR_FAILED4_CAMERA | 0x04 | 4 | 相机打开失败 |
| MR_FAILED4_UNKNOWNREASON | 0x05 | 5 | 未知错误 |
| MR_FAILED4_INVALIDPARAM | 0x06 | 6 | 无效的参数 |
| MR_FAILED4_NOMEMORY | 0x07 | 7 | 内存不足 |
| MR_FAILED4_UNKNOWNUSER | 0x08 | 8 | 没有已录入的用户 |
| MR_FAILED4_MAXUSER | 0x09 | 9 | 录入超过最大用户数量 |
| MR_FAILED4_FACEENROLLED | 0x0A | 10 | 人脸已录入 |
| MR_FAILED4_LIVENESSCHECK | 0x0C | 12 | 活体检测失败 |
| MR_FAILED4_TIMEOUT | 0x0D | 13 | 录入或解锁超时 |
| MR_FAILED4_AUTHORIZATION | 0x0E | 14 | 加密芯片授权失败 |
| MR_FAILED4_READ_FILE | 0x13 | 19 | 读文件失败 |
| MR_FAILED4_WRITE_FILE | 0x14 | 20 | 写文件失败 |
| MR_FAILED4_NO_ENCRYPT | 0x15 | 21 | 通信协议未加密 |